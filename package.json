{
  "name": "idg-teacher-guide",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Inner Development Guide im Schulalltag — Reflexionskarten für den Unterricht",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "generate:share": "node scripts/generate-share-cards.mjs",
    "generate:og": "node scripts/generate-og.mjs",
    "generate:seo": "node scripts/generate-seo-files.mjs",
    "prebuild": "npm run generate:share && npm run generate:og && npm run generate:seo"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.4",
    "sharp": "^0.33.5",
    "vite": "^6.0.7"
  }
}
